name: textvault-dev

services:
  frontend:
    build:
      context: .
      target: app
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
    env_file:
      - .env
    environment:
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "5173:5173"